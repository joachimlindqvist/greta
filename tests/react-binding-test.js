import expect from 'expect';
import { mount } from 'enzyme';

import { connect, StoreProvider } from 'src/index';

describe('connect', () => {
  it('works', () => {
    // const o = <div></div>;
  });
});